// Re-export the UserStatusToggle component for backward compatibility
// Both enable and disable functionality are now handled by the same component

import UserStatusToggle from './status-toggle'

export default UserStatusToggle
